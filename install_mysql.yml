- hosts: vm1
  become: yes
  become_method: sudo

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install MySQL
      apt:
      name: mysql-server=8.0.44-0ubuntu0.24.04.1
        state: present
